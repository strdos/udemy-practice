using System;

namespace Polymorphism
{
    // 'has a' (rather than 'is a') inheritance relationship
    internal class CarIdInfo
    {
        public int IdNum { get; set; } = 0;
        public string Owner { get; set; } = "Unknown";
    }
}
